apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  namespace: devmeet
data:
  grafana.ini: |
    [server]
    root_url = http://localhost:3000
    
    [security]
    admin_user = admin
    admin_password = admin
    
    [users]
    allow_sign_up = false
